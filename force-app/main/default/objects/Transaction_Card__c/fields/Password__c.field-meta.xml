<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Password__c</fullName>
    <externalId>false</externalId>
    <formula>TRIM(UPPER(LEFT(Account_Passenger__r.FirstName, 1))+UPPER(LEFT(Account_Passenger__r.LastName, 1))+LOWER(LEFT(Account_Passenger__r.FirstName, 1))+ LOWER(LEFT(Account_Passenger__r.LastName, 1))+     MID(Name, FIND(&apos;-&apos;, Name)+1  , LEN(Name)))</formula>
    <formulaTreatBlanksAs>BlankAsZero</formulaTreatBlanksAs>
    <label>סיסמא</label>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <trackTrending>false</trackTrending>
    <type>Text</type>
    <unique>false</unique>
</CustomField>
